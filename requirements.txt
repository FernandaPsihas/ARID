# parsers — python/cpp/jsonnet use tree-sitter; fcl uses an in-house recursive-descent parser (parsers/parse_fcl.py), not tree-sitter
tree-sitter==0.25.2
tree-sitter-python==0.25.0
tree-sitter-cpp==0.23.4
# tree-sitter-jsonnet: vendored in-repo with a patched setup.py (adds src/scanner.c,
# which the published 0.0.1 wheel omits). Local path -> pip compiles it from source on
# install, so `pip install -r requirements.txt` Just Works on Windows and Linux. Needs
# a C compiler (build-essential on Linux / MSVC Build Tools on Windows) like any grammar.
./tree-sitter-jsonnet

# embed/store (merged from embed_store.py) — not installed locally yet, unpinned
qdrant-client
ollama

# arid_mcp.py — MCP server exposing retrieval as tools to a remote Claude (over SSH stdio)
mcp
