__pycache__/
*.pyc

# extraction inputs/outputs — not part of ARID history
dunereco/
chunks.jsonl
chunks_partial.jsonl
chunks_partial.manifest.json
chunks_quarkylab.jsonl

# generated eval run reports — regenerate via bench_ab.py/bench_retrieval.py/temp_sweep.py, not source
EGEpipeline/eval/ab_report_*
EGEpipeline/eval/retrieval_report_*
EGEpipeline/eval/temp_sweep_*

# researcher chat sessions from EGEpipeline/chat.py — local usage data, not source
feedback/

*.log

# per-researcher local overrides (project name + host ports). .env.example is tracked.
.env

# local-only, not shipped in the public repo
CLAUDE.md
CHANGELOG.md

# one-time local migration override, not shipped
docker-compose.migrate.yml
